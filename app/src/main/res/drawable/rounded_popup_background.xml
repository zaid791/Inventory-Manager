<!-- res/drawable/rounded_popup_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color -->
    <corners android:radius="16dp" /> <!-- Round corners -->
    <stroke android:width="2dp" android:color="@android:color/black" /> <!-- Border color and width -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
